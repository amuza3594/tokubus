// Stub for 'virtual:pwa-register' used only by the single-file artifact build,
// which doesn't include vite-plugin-pwa (no service worker inside an artifact iframe).
export function registerSW(): void {}
